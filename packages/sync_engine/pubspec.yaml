name: sync_engine
description: Offline-first sync engine with Drift storage and pluggable transport.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.5
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  drift: ^2.28.0
  drift_flutter: ^0.2.5
  sqlite3_flutter_libs: ^0.5.34
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.28.0
  build_runner: ^2.4.15
